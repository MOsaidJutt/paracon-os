import { AdminStub } from "@/components/admin/admin-stub";

export default function AdminRolesPage() {
  return (
    <AdminStub
      title="Roles & Permissions"
      description="Create custom roles and assign permission slugs in a grouped UI."
      phase="Phase 1"
    />
  );
}
