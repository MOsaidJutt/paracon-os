import { AdminStub } from "@/components/admin/admin-stub";

export default function AdminModulesPage() {
  return (
    <AdminStub
      title="Modules"
      description="Toggle product modules per organisation — hides nav and blocks routes when disabled."
      phase="Phase 1"
    />
  );
}
